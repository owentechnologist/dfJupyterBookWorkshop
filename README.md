## instructions for working with jupyter lab in uv with pydantic etc
## assuming you have installed uv ( brew install uv )
uv init
uv add pydantic-ai python-dotenv jupyterlab redis pandas
uv run jupyter lab


